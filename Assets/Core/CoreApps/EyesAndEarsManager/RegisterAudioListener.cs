using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace BP.Core
{
    public class RegisterAudioListener : MonoBehaviour
    {
        //private void OnEnable()
        //{
        //    if (SceneLoader.instance == null) { return; }

        //    var listener = GetComponentInChildren<AudioListener>();

        //    var listenerManager = SceneLoader.instance.GetAudioListenerManagerReference();

        //    if (listenerManager && listener)
        //    {
        //        listenerManager.RegisterNewActiveListener(listener);
        //    }
        //}
    }
}

